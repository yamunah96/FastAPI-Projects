1. Install all the libraries from requirements.txt pip install -r requirements.txt
2. run app.py uvicorn app:app --reload
3. run frontend.py by taking a new terminal in VS Code streamlit run frontend.py
